# ptc.py, bench.py and huffman.py need nothing outside the standard library
# except brotli, which is only a benchmark baseline.
brotli>=1.1.0

# llm_ptc.py only
torch>=2.0
transformers>=4.40
numpy>=1.24

# scripts/make_charts.py only
matplotlib>=3.7
